# Vem flyger?

Cloudflare Pages + Pages Functions version of Flight Wall.

## Endpoints
- `/api/health`
- `/api/aircraft?lat=59.633810&lon=17.915602&radiusKm=5`
- `/api/route?callsign=RYR4618`

Cloudflare Pages should use the repository root as the project root. No build command is required for these static files and Pages Functions.
